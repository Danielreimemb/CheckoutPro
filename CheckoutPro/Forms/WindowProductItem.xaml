<Window x:Class="CheckoutPro.Forms.WindowProductItem"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:colorpicker="clr-namespace:ColorPicker;assembly=ColorPicker"
        xmlns:local="clr-namespace:CheckoutPro.Forms"
        mc:Ignorable="d"
        Title="WindowProductItem" Height="550" Width="740">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2">
            <Rectangle Fill="Green" Height="50"/>
            <TextBlock Text="Produkt hinzufügen" TextAlignment="Center" VerticalAlignment="Center" FontSize="24" FontWeight="Medium"/>
        </Grid>
            
        <Grid Grid.Row="1" Grid.Column="0" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <GroupBox Grid.Row="0" Header="Name">
                <TextBox Text=""/>
            </GroupBox>
            <GroupBox Grid.Row="1" Header="Preis">
                <TextBox Text=""/>
            </GroupBox>
            <GroupBox Grid.Row="2" Header="Gruppe">
                <ComboBox IsEditable="True" Text="Sammlung"/>
            </GroupBox>
            <GroupBox Grid.Row="3" Header="Vorschau">
                
            </GroupBox>
            
            
        </Grid>



        <Grid Grid.Row="1" Grid.Column="1" Margin="5">
            <GroupBox Header="Farbe">
                <colorpicker:StandardColorPicker x:Name="main" Width="200" Height="200" ShowAlpha="False" SelectedColor="Blue" />
            </GroupBox>
        </Grid>

        <Grid Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Margin="5">
            <Button Content="Speichern"/>
        </Grid>    
    </Grid>
</Window>
